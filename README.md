# 🧮 Simple Calculator (HTML • CSS • JavaScript)

A clean, beginner-friendly calculator built using pure **HTML, CSS, and JavaScript**.  
This project was created to understand DOM manipulation, event handling, and basic UI styling.

---

## 🚀 Features
- Basic arithmetic operations: **+, -, ×, ÷, %, AC, DEL**
- Fully responsive UI
- Live input display
- Clean modern design using Google Fonts
- Zero frameworks — only vanilla JS

## 🖥️ Technologies Used
- **HTML5**
- **CSS3**
- **JavaScript (Vanilla)**

---

## ⚙️ How It Works
- Every button click updates a string (`string`)
- JS `eval()` is used to compute results
- `AC` clears all input  
- `DEL` removes last character  
